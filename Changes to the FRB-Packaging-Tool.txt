Changes to the FRB-Packaging-Tool

1. Auto remove desktop shortcut
2. Integrate SP Software List tool to the front of the FRB-Packaging tool and the end of the tool to update statuses.
3. In the Package Helper tab Can we add an area to the top for the ".\Master Template\_SourceFiles\Globals.ps1" so we have a place when using code on what to use in the Tool that will work well with the wrapper
4. Add error catch so it can go into the live Process Log. 
5. Find a way to capture the actions from the wrapper to place into the Live Process Log
6. Make sure globals.ps1 is loaded into the tool so we can utilize the differences
7. When Validation report is triggered, before launching the tool look to see if the report already exists.  If it exists, ask the tech if they want a new report or skip.
8. Remove (APP-NAME_APP-VERSION) Validation.docx and User Acceptance Testing Guide.docx from .\docs folder
9. Change the network location back for the rest of the LSS project
10. Close installed apps window when the validation report is complete.
11. Capture the Opened application with the snagit cross hairs and make it Figure4.jpg
12. That MSI still does the Metadata search and pull if available
13. Ensure when an msi file is detected that the wrapper functions accordingly. 
14. Need a way to address special cases for the Validation document.  I may need a lot of differences to the Validation document my need to be created manually and build a process for that as well.